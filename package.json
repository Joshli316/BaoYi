{
  "name": "baoyi",
  "version": "1.0.0",
  "description": "Bilingual health insurance navigator for international students",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^29.0.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.3"
  }
}
